spring:
  application:
    name: bookVault
  datasource:
    url: jdbc:h2:mem:bookvault
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
